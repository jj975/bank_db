# 🏦 Banking System & Django Blog

Цей репозиторій містить комплекс лабораторних робіт з дисципліни "Web-технології та бази даних". Проект складається з двох частин: SQL-скриптів для банківської системи та веб-застосунку на Django.

## 📂 Структура проекту

* **`scriptssql/`** — SQL запити для PostgreSQL (Лабораторні 2-5).
  * `lab2` — Створення таблиць БД "Banks".
  * `lab3` — Наповнення даними (INSERT).
  * `lab4` — Вибірка даних (SELECT, агрегатні функції).
  * `lab5` — Складні запити (JOIN, Subqueries).
* **`lab6_banking/`** — Веб-застосунок на Django (Лабораторні 6-10).

## 🚀 Технології

* **Backend:** Python 3.12, Django 4.1.7
* **Database:** PostgreSQL 16
* **Frontend:** HTML5, CSS3, Bootstrap 4.6
* **Tools:** Git, pgAdmin 4, VS Code

## 🛠 Як запустити Django проект

1. **Клонуйте репозиторій:**

    ```bash
    git clone [https://github.com/jj975/bank_db.git](https://github.com/jj975/bank_db.git)
    cd bank_db/lab6_banking
    ```

2. **Створіть та активуйте віртуальне середовище:**

    ```bash
    python -m venv myvenv
    .\myvenv\Scripts\activate
    ```

3. **Встановіть залежності:**

    ```bash
    pip install -r requirements.txt
    ```

4. **Налаштуйте змінні оточення:**
    Створіть файл `.env` у папці `mysite` із налаштуваннями вашої БД (див. приклад у `settings.py`).

5. **Запустіть сервер:**

    ```bash
    cd mysite
    python manage.py runserver
    ```

## 📸 Функціонал

* [x] Адмін-панель для керування статтями та категоріями.
* [x] Головна сторінка з новинами та навігацією.
* [x] Детальний перегляд статті з галереєю зображень.
* [x] Адаптивний дизайн (Bootstrap).
